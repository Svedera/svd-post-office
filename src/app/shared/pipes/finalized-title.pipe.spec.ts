import { FinalizedTitlePipe } from './finalized-title.pipe';

describe('FinalizedPipe', () => {
  it('create an instance', () => {
    const pipe = new FinalizedTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
